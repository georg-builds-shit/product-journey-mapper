# Product Journey Mapper — Shopify Product Analytics

Next.js app in `./app/`. Deployed to Vercel.

- Run `cd app && npm run dev` to start locally
- Env vars: use `vercel env pull` inside `./app/`
- Do NOT modify files outside this directory
- Related project: AI Persona Generator (separate directory)

## End of session

After any build, deployment, or debugging session: scan the project docs above and any inline notes written during the session. If there are new gotchas, errors fixed, or patterns discovered that aren't already in `../../shared/learnings/Learnings Index.md` — add them before closing.

## Key docs — read before making changes

| Doc | When to read |
|---|---|
| `architecture/stack.md` | Before adding dependencies or changing infra |
| `architecture/data-model.md` | Before touching DB schema or API data shapes |
| `integrations/klaviyo-integration.md` | Before touching OAuth or any Klaviyo API calls |
| `Product Journey Mapper.md` | Product overview and feature roadmap |
